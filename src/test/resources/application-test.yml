stubrunner:
  stubsMode: LOCAL
spring:
  config:
    import: ""
  main:
    allow-bean-definition-overriding: true
  flyway:
    enabled: false
eureka:
  client:
    enabled: false
payments:
  routing:
    CARD: RAZORPAY
    UPI: CASHFREE
    WALLET: VYAPAR
